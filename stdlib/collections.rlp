(def (map2 func vals)
    (if (null? vals)
        vals
        (+ (list (func (head vals)))
           (map2 func (tail vals)))))

(def (range n) (__range 0 n))

(def (__range curr n)
    (if (eq? curr n)
        '()
        (+ (list curr) (__range (+ curr 1) n))))
